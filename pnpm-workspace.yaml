# Settings home for pnpm >= 11 (package.json "pnpm" field is no longer read).
# This repo is a single package; this file only carries settings, no workspace packages.
allowBuilds:
  esbuild: true
onlyBuiltDependencies:
  - esbuild
