# Settings home for pnpm >= 11 (package.json "pnpm" field is no longer read).
# Settings + workspace packages for pnpm >= 11.
packages:
  - .
  - packages/*
  - examples/*
allowBuilds:
  esbuild: true
onlyBuiltDependencies:
  - esbuild
